## Trigger workflow: test comment
routes:
  /:
    template: home
    data: page.home

collections:
  /media/:
    permalink: /media/{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
